#!/usr/bin/node
function add (a, b) {
  return (a + b);
}
if (isNaN(process.argv[2]) || isNaN(process.argv[3])) {
  console.log('NaN');
} else {
  a = parseInt(process.argv[2]);
  b = parseInt(process.argv[3]);
  res = add (a, b);
  console.log(res);
}
